/** Bump this by hand on release — the one place the app's version comes from.
 *  Read by FileSaveBanner.svelte (in-app display + update check) and by
 *  scripts/postbuild.mjs (stamped download filename + published version.json). */
export const APP_VERSION = '2026.08';
